{
  "name": "super-task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:log": "docker compose -f docker-compose.dev.yaml up nest next",
    "docker:build": "docker compose -f docker-compose.dev.yaml up -d --build ",
    "prisma:build": "pnpm --filter ./nest exec  prisma generate --schema=libs/prisma/schema.prisma  && pnpm --filter ./nest exec  prisma migrate dev --schema=libs/prisma/schema.prisma",
    "docker:dev:down": "docker compose -f docker-compose.dev.yaml down ",
    "docker:dev:remove": "docker compose -f docker-compose.dev.yaml down -v --remove-orphans"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0"
}